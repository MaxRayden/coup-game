/** Re-export para o servidor Node (compat). */
export {
  CLIENT_PORT,
  SERVER_DEV_PORT,
  SERVER_PROD_PORT,
  resolveServerPort,
  devClientUrl,
  serverUrl,
} from "../shared/constants/ports.js";
